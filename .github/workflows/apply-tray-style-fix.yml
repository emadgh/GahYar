name: Apply tray style fix

permissions:
  contents: write

on:
  push:
    branches:
      - fix/tray-icon-style-spacing

jobs:
  apply:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply source changes
        run: python .github/apply-tray-style-fix.py
