# گاه‌یار ۲.۶.۴

## تغییرات

- افزودن نصب برنامه در `Program Files\GahYar` از داخل تنظیمات
- بستن نسخهٔ قابل‌حمل و اجرای خودکار نسخهٔ نصب‌شده پس از نصب
- افزودن حذف برنامه از داخل تنظیمات با حفظ تنظیمات شخصی کاربر
- دریافت تأیید صریح پیش از نصب و حذف
- جایگزینی پیام‌های تأیید نصب و حذف با پنجرهٔ اختصاصی، راست‌چین و هماهنگ با پوستهٔ روشن یا تیرهٔ گاه‌یار
- نمایش دکمهٔ هشدار قرمز برای تأیید حذف و پشتیبانی از Enter و Escape
- افزودن کادر یادآوری در نصب دربارهٔ پورتابل‌بودن برنامه و اختیاری‌بودن Program Files
- مخفی‌شدن دکمهٔ «برو به امروز» هنگام فعال‌بودن تاریخ امروز
- تشخیص مسیر اجرای فعلی و نمایش «بروزرسانی نسخهٔ نصب‌شده» برای جایگزینی نسخهٔ Program Files با فایل فعلی
- مقایسهٔ نسخهٔ واقعی فایل اجراشده با نسخهٔ نصب‌شده و پیشنهاد جایگزینی فقط در صورت جدیدتر بودن فایل فعلی
- افزودن پیام دعوت اختیاری به معرفی گاه‌یار هنگام نصب
- افزودن پیام تشکر، دانلود مجدد و دریافت بازخورد هنگام حذف

## دریافت

فایل `GahYar.exe` را از بخش Assets همین Release دریافت و اجرا کنید. برنامه قابل‌حمل است و به نصب نیاز ندارد.
